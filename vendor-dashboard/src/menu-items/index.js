import dashboard from './dashboard';
import platform from './platform';
import system from './system';
import other from './other';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, platform, system, other]
};

export default menuItems;
